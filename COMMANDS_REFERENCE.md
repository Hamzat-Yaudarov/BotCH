# 📖 Справочник команд для хостинга и обновления

Полный список команд для развертывания и управления SPN VPN Bot на VPS.

---

# 🚀 КОМАНДЫ ДЛЯ ХОСТИНГА

Используйте эти команды **один раз** при первом развертывании.

## Группа 1: Подготовка сервера

```bash
# Подключение к серверу
ssh root@195.133.21.73

# Обновление списка пакетов
apt update

# Установка обновлений системы
apt upgrade -y

# Установка Python 3 и pip
apt install -y python3 python3-pip python3-venv

# Установка Git для управления кодом
apt install -y git

# Установка PostgreSQL клиента (для работы с Neon)
apt install -y postgresql-client

# Проверка версии Python (должна быть 3.8+)
python3 --version

# Проверка версии pip
pip3 --version
```

**Объяснение:**
- `ssh` - подключение к серверу по защищённому каналу
- `apt update` - обновляет информацию о доступных пакетах
- `apt upgrade` - устанавливает обновления безопасности
- `apt install` - устанавливает новые пакеты

---

## Группа 2: Создание пользователя

```bash
# Создание пользователя vpnbot (от root)
useradd -m -s /bin/bash vpnbot

# Переключение на пользователя vpnbot
su - vpnbot

# Просмотр текущей папки
pwd
# Вывод: /home/vpnbot
```

**Объяснение:**
- `useradd` - создаёт нового пользователя
- `-m` - создаёт домашнюю папку
- `-s /bin/bash` - устанавливает bash как оболочку
- `su - vpnbot` - переключается на пользователя (требует выхода с `exit`)

---

## Группа 3: Клонирование проекта

```bash
# Клонирование проекта (находясь в /home/vpnbot)
git clone https://github.com/YOUR_USERNAME/vpn_bot.git

# Переход в папку проекта
cd vpn_bot

# Просмотр файлов
ls -la

# Проверка что находитесь в правильной папке
pwd
# Вывод: /home/vpnbot/vpn_bot
```

**Объяснение:**
- `git clone` - скачивает весь код с GitHub
- `cd` - переходит в папку
- `ls -la` - показывает все файлы (включая скрытые)

---

## Группа 4: Виртуальное окружение Python

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активирование venv (на Linux/Mac)
source venv/bin/activate

# После активирования в начале строки появится "(venv)"
# Пример: (venv) vpnbot@server:~/vpn_bot$

# Проверка что Python в venv активирован
which python
# Вывод: /home/vpnbot/vpn_bot/venv/bin/python

# Деактивирование venv (когда понадобится)
deactivate
```

**Объяснение:**
- `venv` - виртуальное окружение (изолированная копия Python)
- `source` - активирует скрипт для Linux/Mac
- Для Windows: `venv\Scripts\activate.bat`

---

## Группа 5: Установка зависимостей

```bash
# Убедитесь что venv активировано (в начале строки "(venv)")
# Если нет: source venv/bin/activate

# Обновление pip (пакетного менеджера)
pip install --upgrade pip

# Установка всех зависимостей
pip install -r requirements.txt

# Проверка что всё установилось
pip list

# Проверка конкретного пакета
pip show asyncpg
```

**Объяснение:**
- `pip` - пакетный менеджер Python
- `--upgrade` - обновляет пакет на новую версию
- `-r requirements.txt` - устанавливает из файла со списком

---

## Группа 6: Конфигурация

```bash
# Копирование шаблона конфигурации
cp .env.example .env

# Редактирование .env с реальными данными
nano .env

# В nano:
# Ctrl+X - выход
# Y - сохранить
# Enter - подтвердить имя файла

# Просмотр содержимого .env (без редактирования)
cat .env

# Проверка что нужная переменная установлена
grep BOT_TOKEN .env

# Просмотр только значения (скрыть имя переменной)
grep -oP 'BOT_TOKEN=\K.*' .env
```

**Объяснение:**
- `cp` - копирует файл
- `nano` - текстовый редактор
- `cat` - показывает содержимое файла
- `grep` - ищет строку в файле

---

## Группа 7: Тестирование

```bash
# Убедитесь что venv активировано
source venv/bin/activate

# Запуск бота вручную (для тестирования)
python3 vpn_bot.py

# Должен вывести логи без ошибок
# Если ошибка - исправьте и попробуйте снова

# Для выхода нажмите: Ctrl+C
```

**Объяснение:**
- Это важно сделать перед автоматизацией
- Помогает найти ошибки в конфигурации

---

## Группа 8: Systemd сервис

```bash
# Выход из venv (если были в нём)
deactivate

# Выход из пользователя vpnbot (если были в su)
exit

# Теперь вы снова root

# Создание файла systemd сервиса
nano /etc/systemd/system/vpn-bot.service

# Вставьте содержимое из vpn-bot.service

# Перезагрузка конфигурации systemd
systemctl daemon-reload

# Включение автозапуска (при загрузке сервера)
systemctl enable vpn-bot

# Запуск сервиса
systemctl start vpn-bot

# Проверка статуса
systemctl status vpn-bot

# Должно вывести: Active: active (running)
```

**Объяснение:**
- `systemctl` - управление сервисами в Linux
- `daemon-reload` - перечитывает конфиги
- `enable` - включает автозапуск
- `start` - запускает сервис

---

# 🔄 КОМАНДЫ ДЛЯ ОБНОВЛЕНИЯ

Используйте эти команды **каждый раз** когда нужно обновить проект.

## Способ 1: Автоматический (рекомендуется)

```bash
# Подключение к серверу
ssh root@195.133.21.73

# Переход в папку проекта
cd /home/vpnbot/vpn_bot

# Запуск скрипта обновления (делает всё автоматически)
sudo chmod +x update.sh
sudo ./update.sh

# Готово!
```

**Что делает скрипт:**
1. Создает резервную копию
2. Останавливает бота
3. Скачивает новый код (git pull)
4. Обновляет зависимости (pip)
5. Перезапускает бота
6. Проверяет статус

---

## Способ 2: Ручной (если нужны детали)

```bash
# Подключение к серверу
ssh root@195.133.21.73

# Переход в папку проекта
cd /home/vpnbot/vpn_bot

# ===== Остановка бота =====
sudo systemctl stop vpn-bot

# Подождите пока остановится
sleep 2

# ===== Скачивание нового кода =====
git fetch origin
git reset --hard origin/main

# ===== Обновление зависимостей =====
source venv/bin/activate
pip install -r requirements.txt
deactivate

# ===== Запуск бота =====
sudo systemctl start vpn-bot

# ===== Проверка =====
sudo systemctl status vpn-bot

# Просмотр логов
sudo journalctl -u vpn-bot -f
```

**Объяснение каждой части:**

| Команда | Что делает |
|---------|-----------|
| `systemctl stop` | Останавливает бота (чтобы он не конфликтовал с обновлением) |
| `git fetch origin` | Скачивает информацию о новых коммитах |
| `git reset --hard` | Заменяет локальный код на версию с GitHub |
| `pip install -r` | Обновляет зависимости |
| `systemctl start` | Запускает бота |
| `systemctl status` | Проверяет что бот работает |
| `journalctl -f` | Показывает логи в реальном времени |

---

# 📊 УПРАВЛЕНИЕ БОТОМ

Команды для повседневного управления работающим ботом.

## Статус и контроль

```bash
# Проверка статуса бота
sudo systemctl status vpn-bot

# Запуск бота (если он остановлен)
sudo systemctl start vpn-bot

# Остановка бота
sudo systemctl stop vpn-bot

# Перезапуск бота
sudo systemctl restart vpn-bot

# Проверка что бот работает (вывод: active или inactive)
sudo systemctl is-active vpn-bot

# Проверка включен ли автозапуск (вывод: enabled или disabled)
sudo systemctl is-enabled vpn-bot
```

---

## Логирование

```bash
# Просмотр последних 50 строк логов
sudo journalctl -u vpn-bot -n 50

# Просмотр всех логов за сегодня
sudo journalctl -u vpn-bot --since today

# Просмотр логов за последний час
sudo journalctl -u vpn-bot --since "1 hour ago"

# Просмотр только ошибок
sudo journalctl -u vpn-bot -p err

# Просмотр логов в реальном времени (Ctrl+C для выхода)
sudo journalctl -u vpn-bot -f

# Просмотр логов с форматированием времени
sudo journalctl -u vpn-bot -o short-precise

# Экспорт логов в файл
sudo journalctl -u vpn-bot > vpn-bot-logs.txt
```

---

## Конфигурация

```bash
# Просмотр текущего .env файла
cat /home/vpnbot/vpn_bot/.env

# Редактирование .env
nano /home/vpnbot/vpn_bot/.env

# После редактирования - перезапустить бота
sudo systemctl restart vpn-bot

# Проверить что переменная установлена
grep BOT_TOKEN /home/vpnbot/vpn_bot/.env

# Изменить одну переменную без редактирования (пример)
sed -i 's/OLD_VALUE/NEW_VALUE/g' /home/vpnbot/vpn_bot/.env
```

---

## Резервные копии и откат

```bash
# Скачивание резервной копии на локальную машину
# (на локальной машине)
scp root@195.133.21.73:/home/vpnbot/backups/backup_*.tar.gz ~/backups/

# Откат на предыдущую версию (если что-то сломалось)
sudo systemctl stop vpn-bot
cd /home/vpnbot/vpn_bot
tar -xzf /home/vpnbot/backups/backup_FILENAME.tar.gz .
sudo systemctl start vpn-bot

# Просмотр истории коммитов Git
cd /home/vpnbot/vpn_bot
git log --oneline

# Откат на конкретный коммит
git revert <commit_hash>
```

---

## Мониторинг ресурсов

```bash
# Просмотр использования памяти и CPU
top
# Нажмите 'q' для выхода

# Более красивый вид
htop

# Просмотр дискового пространства
df -h

# Размер папки проекта
du -sh /home/vpnbot/vpn_bot

# Размер логов
du -sh /var/log/journal
```

---

# 🎯 БЫСТРЫЕ СЦЕНАРИИ

### Сценарий: Быстрое обновление после git push

```bash
cd /home/vpnbot/vpn_bot && sudo ./update.sh
```

### Сценарий: Просмотр что-то пошло не так

```bash
sudo journalctl -u vpn-bot -n 100 | grep -i error
```

### Сценарий: Полный перезапуск сервера

```bash
sudo reboot
# Подождите 30 секунд
ssh root@195.133.21.73
sudo systemctl status vpn-bot
```

### Сценарий: Отключить бота временно (не отключая автозапуск)

```bash
sudo systemctl stop vpn-bot
sudo systemctl status vpn-bot  # должно быть inactive
```

### Сценарий: Включить обратно

```bash
sudo systemctl start vpn-bot
sudo systemctl status vpn-bot  # должно быть active
```

### Сценарий: Перезагрузить конфигурацию без перезапуска

К сожалению, нельзя просто перезагрузить .env, нужен полный перезапуск:

```bash
sudo systemctl restart vpn-bot
```

---

# ✅ Чеклист стандартных процедур

## При первом развертывании

- [ ] `apt update && apt upgrade -y`
- [ ] `useradd -m -s /bin/bash vpnbot`
- [ ] `su - vpnbot && git clone ...`
- [ ] `python3 -m venv venv && source venv/bin/activate`
- [ ] `pip install -r requirements.txt`
- [ ] `cp .env.example .env && nano .env`
- [ ] `python3 vpn_bot.py` (тестирование)
- [ ] Создать systemd сервис
- [ ] `sudo systemctl enable vpn-bot && start vpn-bot`

## При каждом обновлении

- [ ] `cd /home/vpnbot/vpn_bot`
- [ ] `sudo ./update.sh`
- [ ] `sudo systemctl status vpn-bot`
- [ ] `sudo journalctl -u vpn-bot -f` (проверить логи)

---

Готово! Теперь вы знаете все команды для управления ботом! 🎉
