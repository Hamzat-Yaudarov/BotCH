# 🚀 Инструкция по развёртыванию VPN Bot

Полный гайд по развёртыванию бота на различных платформах с Neon PostgreSQL.

## 📋 Предварительные требования

- Python 3.8+
- Git
- Telegram Bot Token (@BotFather)
- CryptoBot Token
- XUI Panel доступ
- Neon.tech аккаунт (для БД)

## 1️⃣ Подготовка Neon PostgreSQL

### Шаг 1: Создание проекта на Neon

1. Перейдите на [https://console.neon.tech](https://console.neon.tech)
2. Зарегистрируйтесь или логинитесь
3. Нажмите "New Project"
4. Выберите регион (рекомендуется выбрать ближайший)
5. Дождитесь создания проекта

### Шаг 2: Получение строки подключения

1. После создания проекта, нажмите на него
2. Перейдите на вкладку "Connection Details"
3. Скопируйте URI подключения (она выглядит так):
```
postgresql://user:password@host/database?sslmode=require&channel_binding=require
```
4. Сохраните эту строку - она вам нужна для `.env`

### Шаг 3: Проверка подключения (локально)

```bash
pip install psycopg2-binary
python -c "import psycopg2; psycopg2.connect('DATABASE_URL')"
```

## 2️⃣ Локальная разработка

### Шаг 1: Клонирование и установка

```bash
# Клонируем репозиторий
git clone <your-repo-url>
cd vpn_bot

# Создаём виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### Шаг 2: Конфигурация

```bash
# Копируем пример конфигурации
cp .env.example .env

# Редактируем .env
nano .env
```

**Обязательные переменные:**
```env
BOT_TOKEN=ваш_токен_бота
CRYPTOBOT_TOKEN=ваш_cryptobot_токен
XUI_PANEL_URL=https://ваш_ip:2053
XUI_PANEL_PATH=/ваш_путь/panel
XUI_USERNAME=ваше_имя
XUI_PASSWORD=ваш_пароль
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require&channel_binding=require
OWNER_ID=ваш_telegram_id
```

### Шаг 3: Проверка локально

```bash
python main.py
```

Вы должны увидеть логи:
```
📦 Инициализация базы данных...
✅ Подключение к Neon успешно
✅ Таблицы созданы/проверены
🤖 Инициализация бота...
✅ Бот инициализирован, начинаем polling...
```

## 3️⃣ Развёртывание на VPS/Dedicated

### Требования
- Ubuntu 20.04+ или CentOS 8+
- 512MB+ RAM
- 10GB+ диска
- Root доступ

### Шаг 1: Подготовка сервера

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Python и git
sudo apt install python3.10 python3.10-venv git curl wget -y

# Создаём пользователя для бота
sudo useradd -m -s /bin/bash vpnbot
sudo su - vpnbot
```

### Шаг 2: Клонирование проекта

```bash
# Клонируем репозиторий
git clone <your-repo-url> ~/vpn_bot
cd ~/vpn_bot

# Создаём виртуальное окружение
python3.10 -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### Шаг 3: Конфигурация

```bash
# Копируем и редактируем .env
cp .env.example .env
nano .env

# Меняем переменные на реальные значения
```

### Шаг 4: Запуск с systemd

**Создаём файл сервиса:**
```bash
sudo nano /etc/systemd/system/vpnbot.service
```

**Содержимое файла:**
```ini
[Unit]
Description=VPN Telegram Bot
After=network.target

[Service]
Type=simple
User=vpnbot
WorkingDirectory=/home/vpnbot/vpn_bot
Environment="PATH=/home/vpnbot/vpn_bot/venv/bin"
ExecStart=/home/vpnbot/vpn_bot/venv/bin/python main.py
Restart=on-failure
RestartSec=10
StandardOutput=append:/home/vpnbot/vpn_bot/bot.log
StandardError=append:/home/vpnbot/vpn_bot/bot.log

[Install]
WantedBy=multi-user.target
```

**Запускаем сервис:**
```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Запускаем сервис
sudo systemctl start vpnbot

# Проверяем статус
sudo systemctl status vpnbot

# Включаем автозапуск
sudo systemctl enable vpnbot

# Смотрим логи
sudo journalctl -u vpnbot -f
```

### Шаг 5: Мониторинг

```bash
# Проверяем статус сервиса
sudo systemctl status vpnbot

# Смотрим последние логи
tail -f ~/vpn_bot/bot.log

# Перезагружаем бота
sudo systemctl restart vpnbot
```

## 4️⃣ Развёртывание на Railway

### Шаг 1: Подготовка

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий

### Шаг 2: Конфигурация Railway

1. Нажмите "New Project"
2. Выберите "GitHub Repo"
3. Подключите ваш репозиторий

### Шаг 3: Переменные окружения

1. Откройте "Variables" в Railway
2. Добавьте все переменные из `.env.example`:
```
BOT_TOKEN=...
CRYPTOBOT_TOKEN=...
DATABASE_URL=...
и т.д.
```

### Шаг 4: Procfile

Создайте файл `Procfile`:
```
worker: python main.py
```

### Шаг 5: Запуск

Railway автоматически развернёт бота при пуше в GitHub.

## 5️⃣ Развёртывание на Heroku

### Шаг 1: Подготовка

1. Создайте аккаунт на [heroku.com](https://heroku.com)
2. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)

### Шаг 2: Создание приложения

```bash
heroku login
heroku create your-app-name
```

### Шаг 3: Procfile

Создайте файл `Procfile` (если ещё нет):
```
worker: python main.py
```

### Шаг 4: Переменные окружения

```bash
heroku config:set BOT_TOKEN=your_token
heroku config:set DATABASE_URL=your_database_url
heroku config:set CRYPTOBOT_TOKEN=your_token
# и другие переменные...
```

### Шаг 5: Развёртывание

```bash
git push heroku main
heroku ps:scale worker=1
heroku logs --tail
```

## 6️⃣ Развёртывание в Docker

### Создание Dockerfile

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

### Запуск

```bash
# Сборка образа
docker build -t vpnbot .

# Запуск контейнера
docker run -d \
  --name vpnbot \
  -e BOT_TOKEN=your_token \
  -e DATABASE_URL=your_db \
  vpnbot
```

## 7️⃣ Обновление кода в production

### На VPS/Dedicated

```bash
# SSH на сервер
ssh user@your-server

# Переходим в папку проекта
cd ~/vpn_bot

# Останавливаем бота
sudo systemctl stop vpnbot

# Получаем последние изменения
git pull origin main

# Обновляем зависимости (если добавлены новые)
source venv/bin/activate
pip install -r requirements.txt

# Запускаем бота заново
sudo systemctl start vpnbot

# Проверяем статус
sudo systemctl status vpnbot
```

### На Railway/Heroku

Просто сделайте `git push` - платформа автоматически переразвернёт приложение.

## 🔍 Проверка здоровья бота

### Тестирование функциональности

1. Отправьте `/start` боту
2. Проверьте все кнопки меню
3. Проверьте админ команды (если админ):
   - `/newcode TEST 7 5`
   - `/givesub [user_id] 3`

### Проверка БД

```bash
# На сервере
psql "DATABASE_URL"

# Проверяем таблицы
\dt

# Проверяем данные
SELECT * FROM user_clients;
```

## 🚨 Решение проблем

### Бот не подключается к БД

```
❌ Ошибка инициализации БД
```

**Решение:**
1. Проверьте правильность DATABASE_URL
2. Убедитесь что Neon проект активен
3. Проверьте сетевой доступ (firewall)

### Ошибка при создании клиента XUI

```
❌ Ошибка подключения к панели
```

**Решение:**
1. Проверьте XUI_PANEL_URL
2. Проверьте учётные данные (username/password)
3. Убедитесь что панель доступна с сервера

### Платежи не работают

```
❌ CryptoBot error
```

**Решение:**
1. Проверьте CRYPTOBOT_TOKEN
2. Убедитесь что токен активирован
3. Проверьте API статус CryptoBot

## 📊 Мониторинг и логирование

### Просмотр логов

```bash
# На сервере с systemd
sudo journalctl -u vpnbot -f

# Последние 100 строк
sudo journalctl -u vpnbot -n 100

# За последний час
sudo journalctl -u vpnbot --since "1 hour ago"
```

### Ротация логов

Создайте `/etc/logrotate.d/vpnbot`:
```
/home/vpnbot/vpn_bot/bot.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    create 0640 vpnbot vpnbot
    sharedscripts
}
```

## 🔐 Безопасность

### Best Practices

1. **Никогда не коммитьте .env файл**
   ```
   echo ".env" >> .gitignore
   ```

2. **Используйте SSL для БД**
   - DATABASE_URL должен содержать `sslmode=require`

3. **Ограничивайте доступ к серверу**
   - Используйте SSH ключи, не пароли
   - Откройте только необходимые порты

4. **Регулярно обновляйте зависимости**
   ```bash
   pip install --upgrade -r requirements.txt
   ```

5. **Используйте разные токены для разных сред**
   - Production, Staging, Development токены

## 📞 Поддержка

Если возникают проблемы при развёртывании:
1. Проверьте логи приложения
2. Убедитесь что все переменные окружения установлены
3. Проверьте подключение к БД
4. Проверьте API ключи

## ✅ Чек-лист перед production

- [ ] Все переменные окружения установлены
- [ ] DATABASE_URL подключается успешно
- [ ] XUI Panel доступна и авторизация работает
- [ ] CryptoBot токен активирован
- [ ] Бот отвечает на команды
- [ ] Логирование работает
- [ ] Резервные копии БД настроены (если нужно)
- [ ] Мониторинг настроен
- [ ] Error tracking настроен (опционально)

Готово! Бот готов к production! 🎉
