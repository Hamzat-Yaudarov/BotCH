# ⚡ Быстрый старт - Развертывание на VPS

Если вы спешите, используйте эту краткую инструкцию вместо полного HOSTING.md

---

## 🚀 Первый раз (Initial Deploy)

### Всё на одной странице

```bash
# 1. Подключитесь к серверу
ssh root@ВАШ_IP_СЕРВЕРА

# 2. Скачайте скрипт развертывания
cd /tmp
git clone https://github.com/YOUR_USERNAME/vpn_bot.git
cd vpn_bot

# 3. Запустите скрипт (он всё сделает автоматически)
sudo chmod +x deploy.sh
sudo ./deploy.sh

# 4. Когда скрипт попросит - отредактируйте .env
nano /home/vpnbot/vpn_bot/.env

# 5. Перезапустите бота
sudo systemctl restart vpn-bot

# 6. Проверьте что работает
sudo systemctl status vpn-bot
sudo journalctl -u vpn-bot -f
```

**Что делает скрипт:**
✓ Обновляет систему  
✓ Устанавливает Python, Git, PostgreSQL клиент  
✓ Создает пользователя vpnbot  
✓ Клонирует код  
✓ Создает виртуальное окружение  
✓ Устанавливает зависимости  
✓ Создает systemd сервис  
✓ Запускает бота  

---

## 🔄 Обновление (Update Deploy)

Когда нужно обновить код или исправить баги:

```bash
# 1. Подключитесь к серверу
ssh root@ВАШ_IP_СЕРВЕРА

# 2. Перейдите в папку проекта
cd /home/vpnbot/vpn_bot

# 3. Запустите скрипт обновления
sudo chmod +x update.sh
sudo ./update.sh

# Готово! Бот обновлен и перезапущен
```

**Что делает скрипт:**
✓ Создает резервную копию  
✓ Останавливает бота  
✓ Скачивает новый код (git pull)  
✓ Обновляет зависимости  
✓ Перезапускает бота  
✓ Проверяет статус  

---

## 📋 Управление ботом

```bash
# Статус
sudo systemctl status vpn-bot

# Запуск
sudo systemctl start vpn-bot

# Остановка
sudo systemctl stop vpn-bot

# Перезапуск
sudo systemctl restart vpn-bot

# Логи (последние 50 строк)
sudo journalctl -u vpn-bot -n 50

# Логи в реальном времени (Ctrl+C для выхода)
sudo journalctl -u vpn-bot -f

# Логи за последний час
sudo journalctl -u vpn-bot --since "1 hour ago"

# Проверить активен ли
sudo systemctl is-active vpn-bot
```

---

## 🔧 Редактирование конфига

```bash
# Отредактировать .env переменные
nano /home/vpnbot/vpn_bot/.env

# После редактирования - перезапустить
sudo systemctl restart vpn-bot
```

---

## ⚠️ Если что-то сломалось

```bash
# Посмотрите последние ошибки
sudo journalctl -u vpn-bot -n 100

# Проверьте .env заполнен ли
cat /home/vpnbot/vpn_bot/.env

# Проверьте права доступа
ls -la /home/vpnbot/vpn_bot/

# Попробуйте перезапустить
sudo systemctl restart vpn-bot

# Если не помогает - посмотрите полные логи
sudo journalctl -u vpn-bot --since today
```

---

## 📚 Больше информации

Для более подробного руководства смотрите **HOSTING.md**

Там есть:
- Пошаговое объяснение каждого шага
- Решение проблем
- Резервные копии
- Мониторинг
- И многое другое

```bash
# Прочитайте на сервере
less HOSTING.md

# Или на локальной машине
cat HOSTING.md | less
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Я хочу обновить код на GitHub и развернуть его

```bash
# На локальной машине:
git add .
git commit -m "Обновление"
git push origin main

# На сервере:
ssh root@ВАШ_IP
cd /home/vpnbot/vpn_bot
sudo ./update.sh
```

### Сценарий 2: Я хочу перезагрузить сервер и убедиться что бот запустился

```bash
# На сервере:
sudo reboot

# Подождите ~30 секунд

# Проверьте что бот работает:
ssh root@ВАШ_IP
sudo systemctl status vpn-bot
```

### Сценарий 3: Я хочу изменить конфигурацию (токены, IP и т.д.)

```bash
# На сервере:
ssh root@ВАШ_IP
nano /home/vpnbot/vpn_bot/.env

# Отредактируйте нужные значения и Ctrl+X, Y, Enter

# Перезапустите:
sudo systemctl restart vpn-bot

# Проверьте:
sudo journalctl -u vpn-bot -f
```

### Сценарий 4: Резервная копия базы данных

```bash
# На сервере:
pg_dump "$(grep DATABASE_URL /home/vpnbot/vpn_bot/.env | cut -d= -f2)" > /home/vpnbot/backup_$(date +%Y%m%d).sql

# Скопируйте на локальную машину:
# На локальной машине:
scp root@ВАШ_IP:/home/vpnbot/backup_*.sql ~/backups/
```

---

## ✅ Чеклист первого запуска

- [ ] SSH ключи настроены
- [ ] VPS сервер арендован
- [ ] Git репозиторий готов
- [ ] .env.example заполнен правильно
- [ ] Запущен deploy.sh
- [ ] Отредактирован .env с реальными данными
- [ ] Бот запущен (systemctl status vpn-bot)
- [ ] Логи проверены (нет ошибок)
- [ ] Бот отвечает на команды в Telegram
- [ ] Systemd сервис настроен на автозапуск (systemctl enable vpn-bot)

---

Всё готово! 🎉

Если возникнут вопросы - смотрите HOSTING.md или проверяйте логи:
```bash
sudo journalctl -u vpn-bot -f
```
